# Runtime dependencies for FleshNote Backend
# Use this file when running from source code.
# For building/packaging, use requirements_build.txt instead.
#
# Python 3.13 is the supported runtime.
# NOTE: lxml / thinc / blis / numpy arrive transitively via spaCy;
# requirements_build.txt pins them explicitly for reproducible frozen builds.
fastapi==0.129.0
uvicorn==0.41.0
pydantic==2.12.5
python-multipart==0.0.32
spacy==3.8.11
huspacy==0.12.1
python-docx==1.2.0
xhtml2pdf==0.2.17
ebooklib==0.20
nltk==3.9.1
phunspell==0.1.6
pyyaml==6.0.3
cryptography>=42.0
